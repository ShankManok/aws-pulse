{
  "name": "aws-pulse",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "infra",
    "sdk/python"
  ],
  "scripts": {
    "test": "cd infra && npm test",
    "synth": "npm run build --workspace infra && cd infra && cdk synth",
    "deploy": "npm run build --workspace infra && cd infra && cdk deploy --all"
  }
}
